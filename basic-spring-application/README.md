> pom.xml contains the dependency for spring.
> BasicSpringMain class contains the code to load the spring context, get the bean required and display the content of the bean.
> MyBean is a simple pojo class.
> application-beans.xml contains the bean configurations.